using System;
using System.Collections;

using UnityEngine;

namespace VAT.Shared.Utilities {
    /// <summary>
    /// Simple enum for defining an axis.
    /// </summary>
    public enum Axis {
        X = 0,
        Y = 1,
        Z = 2
    }
}